{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pedidodetalle</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pedidoDetalle.id }}</td>
            </tr>
            <tr>
                <th>Productoid</th>
                <td>{{ pedidoDetalle.productoId }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ pedidoDetalle.cantidad }}</td>
            </tr>
            <tr>
                <th>Pedidoid</th>
                <td>{{ pedidoDetalle.pedidoId }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pedidodetalle_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('pedidodetalle_edit', { 'id': pedidoDetalle.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
